# U00｜課前準備

> 本單元為課前自修，不佔正課時間。目標：確保第一堂課不卡在環境問題上。

---

## 安裝清單

| 工具 | 版本 | 推薦安裝方式（macOS） | Windows WSL 注意事項 |
|------|------|----------------------|---------------------|
| Node.js 20 | 20.x LTS | [nvm](https://github.com/nvm-sh/nvm)：`nvm install 20` | WSL2 中同樣使用 nvm 安裝；參考 [Microsoft WSL 安裝指南](https://learn.microsoft.com/zh-tw/windows/wsl/install) |
| Docker Desktop | 最新版 | [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop/) | 安裝 [Docker Desktop for Windows](https://docs.docker.com/desktop/setup/install/windows-install/) 並啟用 WSL2 後端整合 |
| Git | 最新版 | `brew install git` 或 Xcode Command Line Tools | WSL2 內建或 `sudo apt install git` |
| IDE | — | **VS Code**（推薦） | 同 macOS；可搭配 [Remote - WSL 擴充](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-wsl) |

> **作業系統說明**：本教案以 **macOS** 為主要示範環境。Windows 使用者建議透過 **WSL2**（Windows Subsystem for Linux）進行開發，可獲得與 macOS/Linux 一致的命令列體驗。

---

## VS Code 推薦擴充

| 擴充套件 | 用途 |
|----------|------|
| [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint) | JavaScript/TypeScript 程式碼檢查 |
| [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) | 程式碼格式化 |
| [Nunjucks](https://marketplace.visualstudio.com/items?itemName=ronnidc.nunjucks) | Nunjucks 模板語法高亮 |
| [REST Client](https://marketplace.visualstudio.com/items?itemName=humao.rest-client) | 在 VS Code 中直接測試 API |
| [Docker](https://marketplace.visualstudio.com/items?itemName=ms-azuretools.vscode-docker) | Docker 整合 |

---

## 驗證指令

```bash
# 確認 Node.js 版本
node -v                          # 應顯示 v20.x

# 確認 npm 可用
npm -v                           # 應顯示 10.x 或以上

# 確認 Docker Compose 可用
docker compose version           # 應顯示 Docker Compose version v2.x

# Clone 專案並安裝依賴
git clone <本 Repo URL>
cd nodejs-nestjs-sample
npm install                      # 確認能下載依賴

# 啟動容器（MySQL + Redis）
docker compose up -d
docker compose ps                # 確認 db 和 redis 都在 running 狀態
```

---

## 專案結構預覽

完成安裝後，專案目錄應如下：

```
nodejs-nestjs-sample/
├── .devcontainer/               ← Dev Container 設定
├── docker/                      ← Docker 相關檔案
├── docker-compose.yml           ← 容器編排（App + MySQL + Redis）
├── .env.example                 ← 環境變數範本
├── docs/                        ← 教案文件
│   └── 新手入門教案/
│       ├── README.md            ← 教案入口
│       ├── 00-課前準備.md
│       ├── 01-專案啟動與第一支API.md
│       └── ...
└── README.md                    ← 專案說明
```

---

## 踩坑提示

| 現象 | 原因 | 解法 |
|------|------|------|
| `docker: command not found` | Docker Desktop 未啟動或未安裝 | macOS / Windows 需先啟動 Docker Desktop 應用程式 |
| npm install 卡住或逾時 | 公司網路可能封鎖 npm registry | 設定 npm proxy 或切換至個人網路；或使用 `npm config set registry https://registry.npmmirror.com` |
| `node -v` 顯示非 20 | 系統有多版本 Node.js | 用 nvm `nvm use 20` 或設定 `.nvmrc` |
| `docker compose up` 失敗 | Docker Desktop 未啟動或 Port 被占用 | 確認 Docker Desktop 已啟動；用 `lsof -i :3306` 檢查 Port 占用 |
| VS Code 沒有語法高亮 | 未安裝相關擴充套件 | 安裝上述推薦的 VS Code 擴充 |

---

## 驗收標準（DoD）

- [ ] `node -v` 顯示 v20.x
- [ ] `npm -v` 可執行
- [ ] `docker compose version` 可執行
- [ ] `npm install` 成功完成（無錯誤訊息）
- [ ] `docker compose up -d` 能成功啟動容器
- [ ] `docker compose ps` 顯示 db 和 redis 都在 running 狀態
