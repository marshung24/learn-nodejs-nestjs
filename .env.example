# docker-compose 環境變數範本
# 使用方式：cp .env.example .env，再依需求修改

# 專案名稱（會用在容器名稱和 volume 名稱的前綴）
PROJECT_NAME=nestjs-app

# 各服務對外 port（本機已被佔用時可改成其他 port）
APP_PORT=3000       # NestJS 應用
MYSQL_PORT=3306     # MySQL 資料庫
REDIS_PORT=6379     # Redis 快取
